import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uangwarung.app',
  appName: 'Uang Warung',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
